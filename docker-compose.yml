services:
  app:
    build: .
    ports:
      - "3031:8080"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - app-data:/data

volumes:
  app-data:
